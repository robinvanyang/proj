#pragma once

// You may need to build the project (run Qt uic code generator) to get "ui_{{ WindowClassName.lower() }}.h" resolved

#include <QtWidgets/QMainWindow>
#include "ui_{{ WindowClassName.lower() }}.h"

QT_BEGIN_NAMESPACE
namespace Ui { class {{ WindowClassName }}; };
QT_END_NAMESPACE

class {{ WindowClassName }} : public QMainWindow
{
    Q_OBJECT

public:
    {{ WindowClassName }}(QWidget *parent = nullptr);
    ~{{ WindowClassName }}();

private:
    Ui::{{ WindowClassName }} *ui;
};
